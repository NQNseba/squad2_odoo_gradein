<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <!-- FORM -->
        <record id="view_gradein_order_form" model="ir.ui.view">
            <field name="name">Grade In Order Form</field>
            <field name="model">gradein.order</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="name"/>
                        <field name="date"/>
                        <field name="state"/>
                        <field name="equipment_id"/>
                        <field name="imei"/>
                    </group>
                    <group>
                        <field name="partner_id"/>
                        <field name="reject_motive_id"/>   
                        <field name="price"/>  
                        <field name="review"/>  
                        <field name="answer_ids"/>
                        <field name="question_id"/>                         
                    </group>
                    <group>
                        <field name="image_ids"
                        class="o_website_sale_image_list"
                        context="{'default_name' : name}"
                        mode="kanban"
                        add-label="Agregar una imagen"
                        no-label="1"/>
                    </group>
                </form>
            </field>
        </record>

        <!-- TREE -->
        <record id="view_gradein_order_tree" model="ir.ui.view">
            <field name="name">Grade In Order Tree</field>
            <field name="model">gradein.order</field>
            <field name="arch" type="xml">
                <tree string="Orders">
                    <!-- Definimos las columnas que seran visibles -->
                        <field name="name"/>
                        <field name="date"/>
                        <field name="state"/>
                        <field name="equipment_id"/>
                        <field name="imei"/>
                        <field name="partner_id"/>
                        <field name="reject_motive_id"/>
                        <field name="price"/> 
                </tree>
            </field>
        </record>
        
        <!-- SEARCH -->
        <record id="view_gradein_order_search" model="ir.ui.view">
            <field name="name">Grade In Order Search</field>
            <field name="model">gradein.order</field>
            <field name="arch" type="xml">
                <search>
                    <!-- Filtros Rapidos de Busqueda -->
                        <field name="name" string="Nombre"/>
                        <filter name="name" string="Nombre" context="{'group_by': 'name'}"/>
                        <field name="date" string="Fecha"/>
                        <filter name="date" string="Fecha" context="{'group_by': 'date'}"/>
                        <field name="imei" string="IMEI"/>
                        <filter name="imei" string="IMEI" context="{'group_by': 'imei'}"/>
                        <field name="equipment_id" string="Equipo"/>
                        <filter name="equipment_id" string="Equipo" context="{'group_by': 'equipment_id'}"/>
                        <field name="reject_motive_id" string="Motivo de rechazo"/>
                        <filter name="reject_motive_id" string="Motivo de rechazo" domain="[('reject_motive_id.name', '=', 'value')]" context="{'group_by': 'reject_motive_id'}" />


                    <!-- Grupos de estado -->
                    <group expand="0" string="Agrupar por Estado">
                        <filter name="draft" string="Borrador" domain="[('state', '=', 'draft')]"/>
                        <filter name="confirmed" string="Confirmado" domain="[('state', '=', 'confirmed')]"/>
                        <filter name="rejected" string="Rechazado" domain="[('state', '=', 'rejected')]"/>
                    </group>

                </search>
            </field>
        </record>

        <!-- ACTION -->
        <record id="action_gradein_order" model="ir.actions.act_window">
            <field name="name">Grade In Order Action</field>
            <field name="res_model">gradein.order</field>
            <field name="view_mode">tree,form,search</field>
            <field name="view_id" ref="view_gradein_order_tree"/>
        </record>
    
    </data>
    

</odoo>
